#!/usr/bin/env bash

read -n 1 -p 'This will remove all node_modules found on the computer (Y/n): ' confirm
echo ""

if [ $confirm = 'Y' ] || [ $confirm = 'y' ] ; then
  echo "Removing node_modules"
  find . -name "node_modules" -type d -prune -print -exec rm -rf '{}' \;
fi
